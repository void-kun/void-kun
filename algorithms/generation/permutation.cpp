#include <iostream>
#include <vector>

template <typename T> std::vector<T> permutation(std::vector<T> sInput) {
  std::vector<T> result(0, sInput.size());
  return result;
}

int main() {
  std::cout << "hello world! \n";
  return 0;
}
